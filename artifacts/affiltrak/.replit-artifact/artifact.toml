kind = "web"
previewPath = "/"
title = "InfinityAdX"
version = "1.0.0"
id = "artifacts/affiltrak"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20714

[services.development]
run = "pnpm --filter @workspace/affiltrak run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/affiltrak", "run", "build" ]
publicDir = "artifacts/affiltrak/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20714"
BASE_PATH = "/"
